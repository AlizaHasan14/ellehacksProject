<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alizahasan.cfpcalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InfoActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".Rchange" />
        <activity android:name=".Cchange" />
        <activity android:name=".car" />
        <activity android:name=".stove" />
        <activity android:name=".transportationRoutine" />
        <activity android:name=".travel" />
        <activity android:name=".ProfileActivity"></activity>
    </application>

</manifest>